<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:fitsSystemWindows="true"
    android:layout_height="180dp"
    android:background="@drawable/header_min">

    <ImageView
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/drawer_header_img"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"
        android:src="@mipmap/icon"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:layout_toRightOf="@+id/drawer_header_img"
        android:layout_toEndOf="@+id/drawer_header_img"
        android:layout_marginTop="20dp"
        android:id="@+id/drawer_header_title"
        android:textSize="20sp"
        android:layout_marginLeft="20dp"
        android:layout_marginStart="20dp"
        android:textColor="@color/white"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="' No news can go untapped '"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="5dp"
        android:textColor="@color/white"
        android:textStyle="italic"
        android:textSize="14sp"
        android:gravity="center"/>
</RelativeLayout>